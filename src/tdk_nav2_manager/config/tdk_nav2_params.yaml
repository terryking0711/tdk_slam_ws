# 這是唯一被 nav_launch.py 載入的 Nav2 參數檔。config/_unused/ 下的檔案
# 均未使用，詳見該目錄 README。
# bt_navigator
bt_navigator:
  ros__parameters:
    use_sim_time: False
    global_frame: map
    robot_base_frame: base_footprint
    odom_topic: /odom
    # 目前使用 nav2 內建預設 BT (navigate_to_pose_w_replanning_and_recovery.xml)。
    # 若日後要換自訂 XML,參數名須為 default_nav_to_pose_bt_xml(Humble 起的正確
    # 名稱;default_bt_xml_filename 是 Foxy/Galactic 舊名,Humble 下會被靜默忽略,
    # 不會報錯,只是不生效),且必須給絕對路徑。

    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node

# Controller：MPPI（Omni）
controller_server:
  ros__parameters:
    use_sim_time: False
    controller_frequency: 20.0      # CPU 充裕,20Hz 讓 MPPI 暖啟動更頻繁,輸出更平滑

    # 低於此閾值的速度視為 0。麥輪要保留 y！不可設 0。
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.001
    min_theta_velocity_threshold: 0.001

    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.05
      # ★改 10→15:整體速度降 1/3 後移動變慢,避免被誤判 stuck 觸發 recovery。
      movement_time_allowance: 15.0

    general_goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.07           # 7 cm
      yaw_goal_tolerance: 0.14          # ~8°
      stateful: True                    # xy 達標後只檢查 yaw,防止來回追 xy

    FollowPath:
      plugin: "nav2_mppi_controller::MPPIController"
      motion_model: "Omni"             # ★ 麥輪/全向輪用 Omni（會輸出 vy）

      time_steps: 56                    # 56 * 0.05 = 2.8 s 預測視野
      model_dt: 0.05                    # 建議值 = 1 / controller_frequency(現為20Hz)
      batch_size: 2000

# --- 取樣標準差：根據 $std \approx v_{max} / 2$ 左右調整，保持靈敏度且不浪費採樣點 ---
      vx_std: 0.20
      vy_std: 0.20
      wz_std: 0.30

      # --- 速度上限（已調高至 1.0）---
      vx_max:  0.8
      vx_min: -0.8
      vy_max:  0.8                     # 橫移速度與直行對齊
      vy_min: -0.8
      wz_max:  0.60

      # --- 加速度上限：根據 1.1 ~ 1.2 秒內煞停的物理特性調高 ---
      ax_max: 0.4                       # 原 0.6 -> 提高到 0.9，確保極速 1.0 可以在 1.1 秒內煞停
      ax_min: -0.4
      ay_max: 0.3                       # 麥輪橫移需要足夠的爆發力克服摩擦力
      ay_min: -0.3
      az_max: 0.6                       # 旋轉加速度保持 1.0，反應較靈敏
      
      temperature: 0.3
      gamma: 0.05

      # 預測視野 = time_steps * model_dt * vx_max ≈ 56*0.05*0.8 = 2.24m，
      # prune_distance 必須涵蓋這個範圍，否則取樣軌跡衝出參考路徑末端，
      # PathAlign/PathFollow 的 cost 在末端飽和變平，優化器沒有梯度可跟隨
      prune_distance: 2.5
      transform_tolerance: 0.3
      visualize: false

      retry_attempt_limit: 3
      regenerate_noises: false          # true 會讓每輪噪聲失去時間相關性，放大 chattering

      critics: [
        "ConstraintCritic", "CostCritic", "GoalCritic",
        "GoalAngleCritic", "PathAlignCritic", "PathFollowCritic",
        "TwirlingCritic"
      ]

      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0

      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        critical_cost: 300.0
        consider_footprint: true
        collision_cost: 1000000.0
        near_collision_cost: 253
        near_goal_distance: 0.5
        trajectory_point_step: 2

      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 6.0
        threshold_to_consider: 1.0        # 必須 = PathFollowCritic.threshold_to_consider，避免空窗

      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        threshold_to_consider: 0.60       # 必須 = PathAlignCritic.threshold_to_consider，避免空窗

      # 麥輪沒有「正面」概念,禁用前進偏好,才能自由橫移/斜走。
      PreferForwardCritic:
        enabled: false

      PathAlignCritic:
        enabled: true
        cost_power: 1
        cost_weight: 1.5
        max_path_occupancy_ratio: 0.10
        trajectory_point_step: 4
        threshold_to_consider: 0.6
        offset_from_furthest: 20

      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        offset_from_furthest: 8
        threshold_to_consider: 1.0

      PathAngleCritic:
        enabled: false
        cost_power: 1
        cost_weight: 2.0
        offset_from_furthest: 4
        threshold_to_consider: 0.5
        max_angle_to_furthest: 1.0
        mode: 1

      TwirlingCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0

# Planner：Smac 2D（全向,不考慮車頭,適合麥輪）
planner_server:
  ros__parameters:
    use_sim_time: False
    expected_planner_frequency: 1.0

    planner_plugins: ["GridBased"]

    GridBased:
      plugin: "nav2_smac_planner/SmacPlanner2D"
      tolerance: 0.05
      downsample_costmap: false
      downsampling_factor: 1
      allow_unknown: false
      max_iterations: 1000000
      max_on_approach_iterations: 1000
      max_planning_time: 2.0
      cost_travel_multiplier: 2.0
      use_final_approach_orientation: false

      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true

smoother_server:
  ros__parameters:
    use_sim_time: False
    smoother_plugins: ["simple_smoother"]

    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: True

# Behavior（recovery 動作）
behavior_server:
  ros__parameters:
    use_sim_time: False
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins:
      - spin
      - backup
      - drive_on_heading
      - wait

    spin:
      plugin: "nav2_behaviors/Spin"
    backup:
      plugin: "nav2_behaviors/BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors/DriveOnHeading"
    wait:
      plugin: "nav2_behaviors/Wait"

    global_frame: odom
    robot_base_frame: base_footprint
    transform_tolerance: 0.3
    simulate_ahead_time: 2.0
    max_rotational_vel: 0.6            # ★改 0.45→0.60:與新 wz_max 對齊
    min_rotational_vel: 0.10            # ★改 0.15→0.10
    rotational_acc_lim: 0.5             # ★保持不變: recovery 旋轉加速度也維持

# Local Costmap（滾動視窗）
local_costmap:
  local_costmap:
    ros__parameters:
      use_sim_time: False
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: odom
      robot_base_frame: base_footprint
      rolling_window: true

      # 6x6:MPPI 預測視野 ≈2.24m,須涵蓋在 costmap 視窗內,否則衝出視窗的
      # 取樣點會被 CostCritic 當成碰撞,懲罰所有較快的軌跡
      width: 6
      height: 6
      resolution: 0.05

      # 40cm x 40cm 方形車體。前提:base_footprint 在車體幾何中心。
      footprint: "[[-0.20, -0.20], [-0.20, 0.20], [0.20, 0.20], [0.20, -0.20]]"
      footprint_padding: 0.02

      plugins:
        - obstacle_layer
        - inflation_layer

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 4.0
          raytrace_min_range: 0.0
          obstacle_max_range: 3.0
          obstacle_min_range: 0.0

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: True
        inflation_radius: 0.30
        cost_scaling_factor: 4.0

      always_send_full_costmap: True

# Global Costmap
global_costmap:
  global_costmap:
    ros__parameters:
      use_sim_time: False
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_footprint

      footprint: "[[-0.20, -0.20], [-0.20, 0.20], [0.20, 0.20], [0.20, -0.20]]"
      footprint_padding: 0.02
      resolution: 0.05
      track_unknown_space: true

      plugins:
        - static_layer
        - obstacle_layer
        - inflation_layer

      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True

      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 4.0
          raytrace_min_range: 0.0
          obstacle_max_range: 3.0
          obstacle_min_range: 0.0

      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: True
        inflation_radius: 0.30
        cost_scaling_factor: 3.0

      always_send_full_costmap: True

# Waypoint Follower
waypoint_follower:
  ros__parameters:
    use_sim_time: False
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"

    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

# Velocity Smoother（★ 全檔唯一一個,數值已與 MPPI 對齊）
velocity_smoother:
  ros__parameters:
    use_sim_time: False
    smoothing_frequency: 20.0           # 保持 20Hz:負擔極輕,輸出到底盤更平滑
    scale_velocities: false

    # OPEN_LOOP：用上一筆指令估目前速度（模擬常用）。
    # 實車若 odom 準，可改 CLOSED_LOOP 讓平滑更貼實際。
    feedback: "OPEN_LOOP"

# [X, Y, Theta] ★改:與 MPPI 新上限完全對齊。麥輪務必保留 Y!
    max_velocity: [0.80, 0.80, 0.6]   # X, Y 調高至 0.80，Theta 降至 0.6
    min_velocity: [-0.80, -0.80, -0.6]
    max_accel: [0.4, 0.3, 0.6]        # ★ 加速度同步調高至 0.9，確保高速時煞得住
    max_decel: [-0.4, -0.3, -0.6]      # 減速度與加速度對齊

    odom_topic: /odom
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 1.0

# Lifecycle Manager
lifecycle_manager_navigation:
  ros__parameters:
    use_sim_time: False
    autostart: True
    node_names:
      - controller_server
      - smoother_server
      - planner_server
      - behavior_server
      - bt_navigator
      - waypoint_follower
      - velocity_smoother